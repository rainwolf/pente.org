.idea
dockerMain/redis/
localhost.cer
.DS_Store
deploy
deployClasses
letsencrypt/pente.org/
tmpTools
liveGameRoom
renewSSL
updateDevelopment
updateDevelopmentHTMLjs
updateProduction
updateProductiontHTMLjs
docker*/db/
dockerMain/config/
dockerMain/logs/
dockerMain/etctomcat9/
# dockerMail: the config files are tracked (see dockerMail/README.md).
# Only the secrets are ignored.
dockerMail/opendkim/keys/**/*.private
dockerMail/postfix/sasl_passwd*
# Distro-managed postfix internals and editor backups left over from the old
# /etc snapshot. Not tracked, not copied into the image, safe to delete.
dockerMail/postfix/dynamicmaps.cf
dockerMail/postfix/dynamicmaps.cf.d/
dockerMail/postfix/makedefs.out
dockerMail/postfix/post-install
dockerMail/postfix/postfix-files
dockerMail/postfix/postfix-files.d/
dockerMail/postfix/postfix-script
dockerMail/postfix/sasl/
dockerMail/postfix/*.save
dockerMail/postfix/*--
.env
*.sql.gz
settings.jar
testMail
MyDSKeyStore.jks
*.crt
*.key
*.p12
dsg_src/db2/
config/
pentai/
dsg_src/mmai/mmai_player

# opendkim signing keys — mail.private is the DKIM private key.
# Named .private, so the *.key / *.crt / *.p12 rules above do not catch it.
dockerMail/opendkim/keys/
*.private
